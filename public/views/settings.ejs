<!DOCTYPE html>
<html lang="en" class="h-full w-full">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Settings | Events</title>
  <link href="/public/css/output.css" rel="stylesheet">
</head>
<body class="h-full w-full">
  <div class="container mx-auto flex flex-col items-center justify-center min-h-screen px-2 sm:px-0">
    <h2 class="text-2xl sm:text-3xl font-extrabold mb-6 text-white drop-shadow text-center break-words">Account Settings</h2>
    <form id="settingsForm" class="card card-border border-base-300 p-4 sm:p-8 w-full max-w-xl bg-base-100 shadow-2xl">
      <fieldset class="mb-8">
        <legend class="font-bold mb-4 text-lg text-primary">Email Alerts (SMTP)</legend>
        <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
          <div>
            <label class="label">SMTP Host</label>
            <input type="text" name="smtp_host" class="input input-bordered w-full" placeholder="smtp.example.com" />
          </div>
          <div>
            <label class="label">SMTP Port</label>
            <input type="number" name="smtp_port" class="input input-bordered w-full" placeholder="587" />
          </div>
          <div>
            <label class="label">SMTP User</label>
            <input type="text" name="smtp_user" class="input input-bordered w-full" placeholder="user@example.com" />
          </div>
          <div>
            <label class="label">SMTP Password</label>
            <input type="password" name="smtp_pass" class="input input-bordered w-full" placeholder="Password" />
          </div>
          <div class="md:col-span-2">
            <label class="label">Recipient Email Address</label>
            <input type="email" name="smtp_to" class="input input-bordered w-full" placeholder="recipient@example.com" />
          </div>
        </div>
        <label class="flex items-center gap-2 mt-4">
          <input type="checkbox" name="smtp_enabled" class="checkbox" />
          <span class="text-base">Enable Email Alerts</span>
        </label>
      </fieldset>
      <fieldset class="mb-8">
        <legend class="font-bold mb-4 text-lg text-primary">Discord Notifications</legend>
        <div class="mb-4">
          <label class="label">Discord Webhook URL</label>
          <input type="text" name="discord_webhook" class="input input-bordered w-full" placeholder="https://discord.com/api/webhooks/..." />
        </div>
        <label class="flex items-center gap-2">
          <input type="checkbox" name="discord_enabled" class="checkbox" />
          <span class="text-base">Enable Discord Alerts</span>
        </label>
      </fieldset>
      <button type="submit" class="btn btn-primary mt-2 w-full text-lg">Save Settings</button>
    </form>
    <div id="settingsResult" class="mt-6 text-center text-lg break-words"></div>
    <a href="/dashboard" class="btn btn-soft btn-secondary mt-8 w-full sm:w-auto">Back to Dashboard</a>
  </div>
  <script src="/public/js/settings.js"></script>
</body>
</html>
