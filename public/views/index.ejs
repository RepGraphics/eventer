<!DOCTYPE html>
<html lang="en" class="h-full w-full p-8">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <link href="/public/css/output.css" rel="stylesheet">
  <link rel="manifest" href="/public/manifest.json">
  <meta name="theme-color" content="#2563eb">
  <title>Login | Events</title>
</head>
<body class="h-full w-full p-8">
  <div class="container mx-auto flex flex-col items-center justify-center h-full">

    <form id="loginForm" class="p-6">
      <fieldset class="fieldset bg-base-100 card card-border border-base-300 rounded-box w-xs p-4">
        <legend class="fieldset-legend">Login</legend>
      
        <label class="label">Username</label>
        <input type="text" id="loginUsername" class="input" placeholder="Username" />
      
        <label class="label">Password</label>
        <input type="password" id="loginPassword" class="input" placeholder="Password" />
      
        <button type="submit" class="btn btn-soft btn-primary mt-4">Login</button>
      </fieldset>
    </form>
  </div>

  <script src="/public/js/app.js"></script>
  <script>
    if ('serviceWorker' in navigator) {
      window.addEventListener('load', function() {
        navigator.serviceWorker.register('/public/service-worker.js');
      });
    }
  </script>
</body>
</html>
